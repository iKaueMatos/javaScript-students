const frutas = ['banana','morango','Uva'];
const frase = 'Isso e javascript';

//Basicamente estamos percorrendo uma array de objetos ou seja uma NodeList
for(const fruta of frutas) {
    console.log(fruta);
}
